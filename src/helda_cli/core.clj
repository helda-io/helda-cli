(ns helda-cli.core
  (:gen-class)
  (require
    [helda-cli.dsl.entities.browse :as entities]
    [helda-cli.dsl.models.browse :as models]
    )
  )



(defn -main [& args]
  (println "Please use lein repl")
  )
